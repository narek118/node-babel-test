const config = {
    "port": process.env.PORT || 3000,
    "db": {
        "host": "localhost",
        "user": "Narek",
        "password": "password",
        "database": "my_database"
    }
};

export default config;
